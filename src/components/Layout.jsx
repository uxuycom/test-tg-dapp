import { useMatches, Outlet, useNavigate, useLocation, useRoutes, useMatch } from 'react-router-dom'
import { useEffect, useMemo, useRef, useState } from 'react'

export default function () {

    return (
      <>
          <Outlet />
      </>
    )
}